import { GetMatierePipe } from './get-matiere.pipe';

describe('GetMatierePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMatierePipe();
    expect(pipe).toBeTruthy();
  });
});
